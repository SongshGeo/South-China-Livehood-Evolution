{
  "trailingSlash": true,
  "buildCommand": "python3 -m pip install poetry && poetry install --only docs && poetry run mkdocs build",
  "installCommand": "python3 -m pip install --upgrade pip",
  "outputDirectory": "site",
  "framework": null
}

